apiVersion: v1
kind: Service
metadata:
  namespace: shopping-demo
  name: toys
spec:
  ports:
  - name: "http"
    port: 8081
    targetPort: 8081
  selector:
    app: shopping-demo
    component: toys
